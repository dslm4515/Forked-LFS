# Configure source:
./configure --prefix=/usr    \
            --disable-static \
            --docdir=/usr/share/doc/mpc-1.2.1

# Compile
make
make html

# Test
make check

# Install
make install
make install-html
